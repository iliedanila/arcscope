node_modules/
dist/
*.log

# arcscope: ignore the local cache (index/usage/anchors); commit the knowledge
.arcscope/*
!.arcscope/assertions.yaml

# machine-specific MCP registration (regenerate with `arcscope init`)
.mcp.json

# scratch / de-risking spikes (local only)
spike/

# internal design/spec docs — kept locally, not published to the public repo
docs/

# internal agent-instruction files (roadmap, strategy, local paths) — local only
CLAUDE.md
AGENTS.md

# editor / OS
.DS_Store
.idea/
.vscode/
